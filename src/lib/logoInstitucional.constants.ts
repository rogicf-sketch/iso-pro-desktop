/**
 * Logo institucional embutido na aplicacao (relatorios HTML, etiquetas, etc.).
 * Ficheiro em `public/` — mesmo modelo visual da chapa da sidebar.
 * Caminho relativo (`./`) para funcionar com Vite `base: './'` e Electron (`file://`).
 */
export const LOGO_INSTITUCIONAL_PADRAO_FABRICA = './logo-institutional-default.svg';
