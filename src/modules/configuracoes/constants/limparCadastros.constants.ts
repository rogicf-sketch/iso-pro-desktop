/** Confirmacao para limpar apenas cadastros no browser (mantem configuracoes, sessao e utilizadores locais). */
export const LIMPAR_CADASTROS_FRASE_LOCAL = 'APAGAR_CADASTROS_LOCAL';

/** Confirmacao para limpar apenas cadastros na nuvem (mantem configuracoes no snapshot, licencas e contas). A Edge afecta so o tenant activo. */
export const LIMPAR_CADASTROS_FRASE_NUVEM = 'APAGAR_CADASTROS_NUVEM';
